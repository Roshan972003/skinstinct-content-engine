walked the manufacturing floor today and watched them run the viscosity check between batches - they reject a batch if it's outside +/-8% of target viscosity even if everything else (pH, CoA) passes, because viscosity drift usually means something upstream shifted (mixing time, temperature) that the other tests won't catch yet. it's a proxy test. most people have never heard of a brand rejecting a batch over viscosity alone
